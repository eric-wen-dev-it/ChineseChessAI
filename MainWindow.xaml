<Window x:Class="ChineseChessAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChineseChess AlphaZero Trainer" Height="700" Width="1000">
    <Window.Resources>
        <Style x:Key="ChessPieceStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5DC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="OuterCircle" Fill="{TemplateBinding Background}" Stroke="#5D4037" StrokeThickness="2"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Rectangle x:Name="HighlightBox" StrokeThickness="3" Visibility="Collapsed" Margin="-2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Content" Value="">
                                <Setter TargetName="OuterCircle" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="From">
                                <Setter TargetName="HighlightBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="HighlightBox" Property="Stroke" Value="#FF4500"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="To">
                                <Setter TargetName="HighlightBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="HighlightBox" Property="Stroke" Value="#00FF7F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#333333">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Viewbox Grid.Column="0" Grid.Row="0" Margin="20" Stretch="Uniform">
            <Border Background="#D2B48C" BorderBrush="#5D4037" BorderThickness="4" CornerRadius="2" Width="600" Height="660">
                <Grid Margin="10">
                    <Canvas x:Name="ChessLinesCanvas" IsHitTestVisible="False" />
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock Text="楚 河" FontSize="32" FontWeight="Bold" Foreground="#5D4037" Margin="0,0,120,0" Opacity="0.4"/>
                        <TextBlock Text="汉 界" FontSize="32" FontWeight="Bold" Foreground="#5D4037" Opacity="0.4"/>
                    </StackPanel>
                    <UniformGrid x:Name="ChessBoardGrid" Rows="10" Columns="9" Background="Transparent"/>
                </Grid>
            </Border>
        </Viewbox>

        <Grid Grid.Column="1" Grid.Row="0" Margin="10,20,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#555" BorderThickness="1" Margin="0,0,0,10" Padding="10" CornerRadius="5" Background="#2A2A2A">
                <StackPanel>
                    <TextBlock Text="⚙️ 联赛全局配置" Foreground="White" FontWeight="Bold" Margin="0,0,0,10" FontSize="14"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="80"/></Grid.ColumnDefinitions>
                        <TextBlock Text="种群规模 (Population):" Foreground="#CCC" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding PopulationSize, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="#444" Foreground="White" BorderThickness="0" Height="22"/>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <Button x:Name="StartLeagueBtn" Content="万王之王联赛" Height="40" FontSize="15" FontWeight="Bold" Click="OnStartLeagueClick" Background="#E91E63" Foreground="White" Margin="0,0,0,8"/>
                <Button Content="恢复自动观战" Height="32" FontSize="13" Click="OnReplayLastClick" Background="#2196F3" Foreground="White" Margin="0,0,0,8"/>
                <Button Content="导入数据集 (PGN/TXT)" Height="28" FontSize="12" Click="OnLoadDatasetClick" Background="#9C27B0" Foreground="White" Margin="0,0,0,8"/>
                <Button Content="📁 打开 JSON 棋谱" Height="32" FontSize="13" FontWeight="Bold" Click="OnOpenMasterJsonClick" Background="#607D8B" Foreground="White"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="正在观战: #" Foreground="SkyBlue" FontSize="14" FontWeight="Bold"/>
                    <TextBlock x:Name="GameIdLabel" Text="---" Foreground="SkyBlue" FontSize="16" FontWeight="Bold"/>
                    <TextBlock x:Name="GameResultLabel" Text="" Foreground="#FFD700" FontSize="14" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                    <TextBlock Text="对局进度: " Foreground="White" FontSize="13"/>
                    <TextBlock x:Name="StepProgressLabel" Text="0 / 0" Foreground="#00FF7F" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="距离平局还剩: " Foreground="#CCC" FontSize="11"/>
                    <TextBlock x:Name="RemainingStepsLabel" Text="0" Foreground="Orange" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text=" 步" Foreground="#CCC" FontSize="11" VerticalAlignment="Bottom"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Row="3" Text="训练日志:" Foreground="White" Margin="0,5,0,2"/>
            <TextBox x:Name="LogBox" Grid.Row="4" VerticalScrollBarVisibility="Auto" IsReadOnly="True" 
                     Background="Black" Foreground="Lime" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"
                     TextChanged="OnLogBoxTextChanged"/>
        </Grid>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="20,0,20,15">
            <TextBlock Text="标准化 UCCI 棋谱:" Foreground="White" Margin="0,0,0,2" FontSize="12"/>
            <TextBox x:Name="MoveListLog" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                     IsReadOnly="True" Background="#222" Foreground="White" Padding="8" FontSize="11" FontFamily="Consolas"/>
        </StackPanel>
    </Grid>
</Window>
